import { MainCarousel } from "@/components/MainCarousel";
const Home = () => {
  return (
    <main className="h-screen overflow-hidden">
      <MainCarousel />
    </main>
  );
};

export default Home;
